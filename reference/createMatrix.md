# Survey name matching function used when pull from the data warehouse

Survey name matching function used when pull from the data warehouse

## Usage

``` r
createMatrix()
```

## See also

Other helper function: [`Format.AKSlope.fn()`](Format.AKSlope.fn.md),
[`check_dir()`](check_dir.md), [`check_survey()`](check_survey.md),
[`combine_tows()`](combine_tows.md), [`filter_pull()`](filter_pull.md)

## Author

Chantel Wetzel based on code by John Wallace
