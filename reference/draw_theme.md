# A ggplot2 theme for drawing maps

A ggplot2 theme for drawing maps

## Usage

``` r
draw_theme(size = 0.25)
```

## Arguments

- size:

  A real number specifying the line width, or size, for the panel border
  around the figure. The default is `0.25`.
