#' @param month Default NA. A specific numerical month value (e.g., 7 for July) can
#' be passed to the function. This value will be printed out in the "month" column in 
#' the output file formated for Stock Synthesis. The month value should represent 
#' approximately the month that the data were collected. As example if data are collected from
#' June through August, entering a value of 7 corresponding to July would indicate the mean month.
#' Only one month can be input for a specific data source in Stock Synthesis and this value
#' needs to be consisentent across data types (indices, lengths, ages).