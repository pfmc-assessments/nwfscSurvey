#' @param dir Default NULL. User defined directory where output files will be saved. If 
#' left blank (not used in the function call) no output files will be saved. The dir function
#' input is used along with the printfolder function input where is dir is passed to the
#' function and printfolder not defined the default of "forSS" will be used where files will
#' be saved as file.path(dir, printfolder).