#' @param partition Default 0. Partition code based on Stock Synthesis definitions
#' to indicate if the composition data represent all observations (partition = 0),
#' the discarded (partition = 1), or the retained compositions (partition = 2). Survey
#' data should typically have a partition value of 0. This value is printed in the 
#' formatted composition data file and does not impact how the data are expanded.