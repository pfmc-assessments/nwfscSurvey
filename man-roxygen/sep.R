#' @param sep A string used to paste together other strings using
#'   [paste()].  We recommend using a distinct, i.e., rarely used,
#'   string such that pattern matching without unnecessary false positives
#'   is easy. For example, when do you ever see `..` in a column name?
#'   The default is `"_"`.
